# Copilot Instructions — RaceTrack

All project instructions live in [`AGENTS.md`](../AGENTS.md) at the repo root — read it first. It is the single source of truth (project overview, directory map, `bd` task tracking and session close protocol, commands, skills); do not duplicate its content here.

Copilot-specific pieces: workspace prompts in `.github/prompts/`, custom agents in `.github/agents/`, hooks in `.github/hooks/`. See `docs/ai-workflows.md` for the workflow map and layer model.
