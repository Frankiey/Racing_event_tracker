/**
 * @deprecated — Merged into time.ts. Import from './time' instead.
 * This barrel exists only for backwards compatibility.
 */
export {
  countryFlag,
  formatDateRange,
  formatLocalDatetime,
  formatLocalTime,
  formatLocalValue,
  formatUtcValue,
  isPastEvent,
  isPlaceholderTime,
} from './time';
export type { LocalTimeFormat } from './time';